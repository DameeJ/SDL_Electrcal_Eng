[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Ensures /.netlify/functions/parse is reachable exactly as index.html calls it.
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
